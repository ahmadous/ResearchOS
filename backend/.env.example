# --- Flask ---
FLASK_CONFIG=dev
SECRET_KEY=change-me-in-prod
JWT_SECRET_KEY=change-me-in-prod

# Clé Fernet pour chiffrer les clés API stockées (générer en prod) :
#   python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
CREDENTIAL_KEY=

# --- Base de données ---
# Dev (défaut) : SQLite. Prod : PostgreSQL.
# DATABASE_URL=postgresql+psycopg2://researchos:researchos@localhost:5432/researchos
DATABASE_URL=sqlite:///researchos.db

# --- Redis / Celery (Phase 6) ---
REDIS_URL=redis://localhost:6379/0
