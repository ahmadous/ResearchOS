# =====================================================================
#  ResearchOS — dépendances CORE (requises)
#  Le socle fonctionne 100% en LOCAL avec Ollama : aucun SDK cloud requis.
#  Les fournisseurs cloud (OpenAI, Claude, Gemini) sont OPTIONNELS :
#  voir requirements-cloud.txt
# =====================================================================

# --- Core web ---
Flask>=3.0,<4
flask-cors>=5.0
python-dotenv>=1.0

# --- Database ---
SQLAlchemy>=2.0,<3
Flask-SQLAlchemy>=3.1
alembic>=1.13
# psycopg2-binary>=2.9        # requis seulement pour PostgreSQL (prod)

# --- Auth ---
Flask-JWT-Extended>=4.6

# --- Realtime & async (Phase 6) ---
Flask-SocketIO>=5.3
celery>=5.3
redis>=5.0

# --- API docs (Swagger/OpenAPI) ---
flask-smorest>=0.44
marshmallow>=3.20

# --- HTTP client (Ollama + providers compatibles-OpenAI via REST) ---
httpx>=0.27

# --- RAG (Phase 4) ---
# tiktoken>=0.7
# numpy>=1.26

# --- Import de fichiers (extraction de texte) ---
PyMuPDF>=1.24          # PDF (import fitz)
python-docx>=1.1       # Word (.docx)
openpyxl>=3.1          # Excel (.xlsx)
Pillow>=10.0           # images
# python-pptx>=0.6     # PowerPoint (optionnel)

# --- Rapports PDF ---
reportlab>=4.0

# --- Utils ---
tenacity>=8.2
langdetect>=1.0        # détection automatique de la langue du chat
